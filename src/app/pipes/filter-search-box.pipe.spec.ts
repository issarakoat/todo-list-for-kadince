import { FilterSearchBoxPipe } from './filter-search-box.pipe';

describe('FilterSearchBoxPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSearchBoxPipe();
    expect(pipe).toBeTruthy();
  });
});
